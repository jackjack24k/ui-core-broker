import ProductPage from "@/components/ProductPage";
import { customToolsData } from "@/data/productData";

const CustomTools = () => {
  return <ProductPage {...customToolsData} />;
};

export default CustomTools;

