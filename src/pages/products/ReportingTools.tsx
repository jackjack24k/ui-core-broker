import ProductPage from "@/components/ProductPage";
import { reportingToolsData } from "@/data/productData";

const ReportingTools = () => {
  return <ProductPage {...reportingToolsData} />;
};

export default ReportingTools;

