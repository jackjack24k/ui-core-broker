import ProductPage from "@/components/ProductPage";
import { forexCopierData } from "@/data/productData";

const ForexCopier = () => {
  return <ProductPage {...forexCopierData} />;
};

export default ForexCopier;

